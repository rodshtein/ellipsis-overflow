{
  "name": "test",
  "version": "0.0.1",
  "description": "Demo test",
  "main": "src/ellipsis.js",
  "scripts": {
    "build": "node esbuild",
    "dev": "node esbuild --dev",
    "start": "npx derver --production --port=5000 public"
  },
  "author": "Misha Zamkevich",
  "license": "MIT",
  "devDependencies": {
    "derver": "^0.4.11",
    "esbuild": "^0.8.33",
    "esbuild-svelte": "^0.4.0",
    "svelte": "^3.31.2"
  }
}
